# 🛡️ DriverSight v1.2

**DriverSight** — это инструмент для автоматизированного cканирования и инвентаризации модулей ядра Windows. Программа нацелена на выявление легитимных, но уязвимых драйверов (**BYOVD-угроз**), которые могут быть использованы для обхода систем защиты.

---

## 🎯 Ключевые возможности

- **Низкоуровневый сбор данных:** Работает через прямой вызов WinAPI (`EnumDeviceDrivers`), что позволяет видеть реальный список модулей в памяти, минуя стандартные консольные утилиты.
- **Идентификация по SHA-256:** Программа анализирует содержимое файлов, а не их имена. Это позволяет обнаруживать угрозы, даже если файл был переименован для маскировки.
- **Интеллектуальный скоринг:** Автоматическая оценка риска (от 1 до 10) на основе технических возможностей драйвера (например, возможность записи в память ядра).
- **Полная автономность:** Инструмент оптимизирован для работы в закрытых контурах без доступа к интернету.

---

## 🚀 Инструкция по использованию

### 1. Подготовка окружения

Для работы требуются **права администратора**. Если вы запускаете проект из исходного кода, установите зависимости:

```bash
pip install -r requirements.txt
```

### 2. Обновление базы сигнатур

База данных содержит тысячи хешей уязвимых драйверов. Чтобы получить самую свежую версию из сети:

```bash
python main.py --update
```

**Важный нюанс по базе:**

- В скомпилированный `.exe` файл уже **вшита** стабильная версия базы. Программа будет работать «из коробки» даже без интернета.
- Если вы запустили `--update`, программа создаст рядом с собой папку `data/db.json`. С этого момента DriverSight будет приоритетно использовать этот внешний файл как более актуальный.

### 3. Запуск аудита

Для начала сканирования системы просто запустите файл без аргументов:

```bash
python main.py
```

---

## 📊 Результаты и отчеты

Программа выводит краткую таблицу в консоль и генерирует расширенный технический отчет в формате HTML.

- **Уникальность файлов:** Чтобы результаты прошлых проверок не терялись, каждый отчет получает уникальное имя с меткой времени. Пример: `DS_Report_20260119_153022.html`.
- **Содержимое отчета:** Внутри вы найдете полные пути к подозрительным файлам, их хеши, описание конкретной уязвимости (CVE) и прямые ссылки на технические подробности.

---

## 📦 Сборка в единый исполняемый файл (.exe)

Если вы хотите собрать проект в один портативный файл, который можно переносить на другие ПК, используйте **PyInstaller**.

**Команда для сборки в Windows:**

```powershell
python -m PyInstaller --onefile --noconsole --add-data "data/db.json;data" --icon=icon.ico main.py
```

_(Если иконки нет, просто уберите флаг `--icon`)_.

**Параметры команды:**

- `--onefile`: упаковать всё (включая библиотеки) в один EXE.
- `--add-data "data/db.json;data"`: критически важный флаг, который «зашивает» базу данных внутрь файла.

---

## 💡 Оценка рисков (Score)

- **9-10 (Критический):** Драйвер позволяет произвольную запись в память ядра. Критическая угроза для EDR/AV.
- **7-8 (Высокий):** Позволяет чтение памяти ядра. Риск утечки системных секретов.
- **4-6 (Средний):** Уязвимости, приводящие к нестабильности системы (BSOD) или ограниченным утечкам данных.

---

## ⚖️ Этическое использование

Инструмент предназначен для легитимного аудита безопасности и инвентаризации системного ПО. Используйте DriverSight только на оборудовании, доступ к которому разрешен вам владельцем. Автор не несет ответственности за неправомерное использование данного ПО.
